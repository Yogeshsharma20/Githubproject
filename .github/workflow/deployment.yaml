name: Deployment-Workflow_01

on:
  push:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Dependencies
        run: npm ci

      - name: Lint code
        run: npm run lint

      - name: Test code
        run: npm test

      - name: Build code
        run: npm run build

      - name: Deploy code
        run: echo "Deploying code to production server"



    